name: 检查上游更新

on:
  schedule:
    - cron: '0 2,14 * * *'  # 每天 UTC 2:00 和 14:00（北京时间 10:00 和 22:00）
  workflow_dispatch:         # 允许手动触发

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 检查上游更新
        id: check_updates
        run: |
          # 1. 添加上游仓库（带错误处理）
          git remote add upstream https://github.com/yyysuo/mosdns.git 2>/dev/null || echo "上游仓库已存在"
          
          # 2. 获取更新（限制深度）
          git fetch upstream --depth=1 || exit 0
          
          # 3. 比较差异
          git log HEAD..upstream/main --oneline > diff.log || true
          
          # 4. 检查更新
          if [ -s diff.log ]; then
            echo "has_update=true" >> $GITHUB_ENV
            echo "update_count=$(wc -l < diff.log | tr -d ' ')" >> $GITHUB_ENV
            echo "latest_commits<<EOF" >> $GITHUB_ENV
            cat diff.log | head -n 5 >> $GITHUB_ENV
            echo "EOF" >> $GITHUB_ENV
            echo "✅ 检测到 ${update_count} 条上游更新！"
          else
            echo "has_update=false" >> $GITHUB_ENV
            echo "✅ 无新更新（状态正常）"
          fi
          
          # 5. 设置上游仓库链接
          echo "upstream_url=https://github.com/yyysuo/mosdns" >> $GITHUB_ENV
          
          # 6. 设置中文时间格式
          DETECT_TIME=$(date +"%Y-%m-%d %H:%M:%S UTC")
          echo "detect_time=$DETECT_TIME" >> $GITHUB_ENV
          
          # 7. 清理
          rm -f diff.log
          git remote remove upstream 2>/dev/null || true

      - name: 发送 Telegram 通知（仅当有更新时）
        if: env.has_update == 'true'
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          format: markdown
          message: |
            🚨 *【仓库更新提醒】*
            ────────────────
            
            📦 **仓库**： `${{ github.repository }}`
            💡 **状态**：发现 `${{ env.update_count }}` 条新提交
            
            📝 **最新提交**：
            ```
            ${{ env.latest_commits }}
            ```
            
            🔗 [🔍 查看上游仓库](${{ env.upstream_url }})
            ⏰ **检测时间**：${{ env.detect_time }}
            
            ────────────────
            ℹ️ 本通知由 GitHub Actions 自动发送
            📌 无需回复，有问题请检查 Actions 日志
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
